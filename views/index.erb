<div class="container">
  <h2>Pages</h2>

  <% Dir.glob('pages/*.txt') do |page| %>
    <% title_without_path = page.gsub('pages/', '') %>
    <% title = title_without_path.gsub('.txt', '') %>

    <a href="/<%= title %>">
      <p><%= title %></p>
    </a>
  <% end %>
</div>
